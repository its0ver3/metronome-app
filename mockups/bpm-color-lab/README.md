# BPM Color Lab

Standalone prototype for comparing five tempo-slider color and texture directions.

- It does not import production React code or modify application state.
- All prototype files live in this directory.
- Delete `mockups/bpm-color-lab` when the chosen direction has moved into production.
